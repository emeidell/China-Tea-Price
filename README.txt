This project was made just for fun. included in this project is a Express server that
connects  to a API.

Instructions:
clone down
in terminal in folder use git install
use Node.js > node server.js
in URL type: http://localhost:8000